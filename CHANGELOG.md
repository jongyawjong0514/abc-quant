# CHANGELOG

## Unreleased

- 新增全市場 `D-5 / D-3 / D-1` 提早低點日報 `scripts/score_zhu_walkline_early_lowpoint.py` 並串入 scanner。入池資格只看核心分：D-5 必須為月線正斜率、量比 `<=0.50`、小幅拉回且無長上影／持續破底；開收窄幅與下影承接各僅 5 分情境加分，不能靠調高門檻變成隱性硬條件。前 5 日窄幅已改為 `shift(1)`，階段日期使用共同市場交易日曆，停牌缺日不再被錯標；母體限 PIT 產業可辨識個股，排除 ETF。2026-07-13 為 124 檔（D-5 38、D-3 57、D-1 29），其中合格下影 15 檔、`avoid_chase` 4 檔。確認後四項強度仍在獨立主報告排序，不反向合併到提早池。
- 新增四個互斥 D+5 結果、單調累積機率、PIT 產業／流動性／市場狀態及 beta-binomial 部分池化的影子決策面。Holdout 35,854 列的預期報酬模型相關係數僅 0.0013、平均偏誤 +2.09 pct，故標記 `DIAGNOSTIC_UNTRUSTED`，市場／產業 percentile 留空；開啟 holdout 後提出的機率差也只標記未驗證。歷史列集合受標籤成熟度與公司行動篩選，檔名及欄位均明示 `EVALUATION_ONLY_LABEL_FILTERED_UNIVERSE`，不得當成 live 候選池。自由流通市值與歷史概念資料不足，分別維持 `INSUFFICIENT_FEATURES`／`insufficient_data`。
- 新增 `scripts/analyze_zhu_walkline_d10_deep_factors.py`、61 項因果技術因子、48 項嚴格落後法人量比例因子及凍結的 Jan-Feb／Mar／Apr／May-Jun 機率挑戰流程。93,896 個 PIT stock-date 使用共同市場 D+1 開盤到 D+5 收盤、交易成本、同股 5 日冷卻、次日鎖漲停與公司行動排除。每日等 T-1 配額的純技術版 holdout `>=10%` 命中 16.88% vs T-1 11.92%，但尾損 49.42% vs 44.54%、平均淨報酬未顯著改善，不能全面取代。允許棄權的純技術高信心門檻為 976 筆、命中 21.11%、平均 +2.79%、虧損 39.65%，屬回溯型 regime-gated selective shadow challenger；尾損改善未確認、兩日占訊號 46.9%，且為開啟 holdout 後才提升的版本。法人量沒有穩定增益；正式策略／champion／trade effect 不變。
- 新增 PIT 產業分層與小樣本收縮工具 `src/abc_quant/validation/grouped_shadow_validation.py`、`scripts/analyze_zhu_walkline_grouped_validation.py` 及廣泛方法檢討報告。HOLDOUT 35,699 個可交易列可 100% 接到 34 個 `point_in_time_observed_asof` 產業；T-1 有 18/34 個產業少於 20 筆，技術門檻有 23/34 個，故不做原始勝率排行榜。兩邊皆至少 20 筆的 11 產業中，beta-binomial 部分池化後技術門檻命中較高 9 個、平均淨報酬較高 10 個，但仍不足以按產業調參。概念股 H1 為 `insufficient_data`；股本僅 `historical_vintage_unverified` sensitivity。維持 `shadow_observation_only`。
- 新增 `scripts/replay_zhu_walkline_full_market_early_start.py` 與單一凍結規則設定 `config/zhu_walkline_full_market_early_start_replay.yaml`，不再以未來 D 確認事件反推候選，而是一次向量化重播 2026-01～06 的 TWSE/TPEX point-in-time 全市場。225,038 筆 PIT membership 中 225,033 筆與 causal raw-OHLCV features 對齊，來源日／上市日違規均為 0；20 日均額 2,000 萬、至少 60 筆歷史與同股 5 日 cooldown 後產生 995 筆候選。排除 6 筆 forward corporate-action evaluator rows 後，D+5>=10% precision 11.12%，低於全體基準 13.34%（lift 0.83x），balanced accuracy 49.91%、loss 48.33%、中位回報 0.20%；未來 2～10 交易日連到正式 KD 確認僅 6.22%。四項分數 0/25/50/75/100 的 D+5>=10% 為 17.91%/9.28%/10.21%/6.00%/0%，不具單調性，不能用高分補救早期規則。`retrospective_edge_observed=False`、`live_deployable=False`，下一 gate 為 2026-07-14 之後 forward shadow evidence。
- 新增完整 D-10～D 事件軌跡模組 `src/abc_quant/features/early_start_trajectory.py`、有界診斷 `scripts/optimize_zhu_walkline_d10_trajectory.py` 與設定。614 個成熟、排除公司行動、同股 cooldown 事件形成 6,754 列逐日軌跡；1,296 組技術規則中沒有 validation noninferiority pass。選定診斷規則在嚴格 label-maturity purge 後，holdout 保留 126 事件、提醒 15 筆，中位提前 D-4，但 D→D+5 precision 20.00%、recall 11.54%、balanced accuracy 49.77%、loss 40.00%，低於 T-1 基準。另修正評估目標，新增提醒日→同一 D+5 終點回報；該規則中位 13.24%、>=10% 73.33%，只證明事件條件母體內位置較早，不能轉成全市場預測。未 purge 結果明確降級為 exploratory，reuse 會驗證事件鍵與完整 lead grid。
- 每日四項影子報告新增 D-10～D companion trajectory CSV 與 Markdown：每個 D 日新鮮確認候選按個股交易日回看 10 日，逐列重算主力、無上影、量比、融資變化，且所有來源日嚴格早於該觀察日。歷史橫斷面 rank 因未來候選條件化而刻意留空，標記 `retrospective_confirmed_candidates` / `trajectory_live_deployable=False`。2026-07-13 實跑 5 檔×11 日=55 列、55 列四項完整，D 列與主要日報完全一致；dated 與 `latest_zhu_walkline_shadow_strength_trajectory.csv` 均由 scanner runner 自動發布，且不輸出原始融資餘額或外資股數。
- 新增 `scripts/optimize_zhu_walkline_early_start_parameters.py` 與有界搜尋設定 `config/zhu_walkline_early_start_optimizer.yaml`，以完整 2026H1 成熟 KD 確認母體（含 0%～<10% 中性結果）檢驗 T-5／T-3／T-1 提早起漲條件。排除公司行動並套同股 5 交易日 cooldown 後 614 筆，Jan-Feb discovery、Mar-Apr validation、May-Jun untouched holdout，共搜尋 6,570 組。最佳化 T-5 為量比 `<=0.85`，holdout >=10% precision 19.55%、recall 87.50%，但低於現行 T-1 precision 25.93%；最佳化 T-3 為 T-5 量比 `<=0.85` 且 T-3 漲幅 `>2%`，holdout precision 僅 8.82%、loss 64.71%，發生 regime failure。T-1 最佳仍是原條件（T-5 量比 `<=0.75`、T-3/T-1 上漲、T-1 量比 `>=0.70`），因此未驗證能更早且維持準確度；月線斜率、K 轉向、站回月線均未被保留為硬門檻。四項影子強度仍是日常報告主排序，本層只作 watch-only；`blocked_before_promotion_review`、正式 champion/trade effect 不變。
- 將四項影子強度升為所有日期型 Zhu Walkline 報告的預設主要個股排序：`scripts/run_zhu_walkline_shadow.py` 每次寫完 scanner artifacts 後會自動產生日期版與 `latest` 的 strength CSV／JSON／Markdown；版本化門檻固定於 `config/zhu_walkline_shadow_strength_rules.csv`，設定由 `shadow_strength_report.enabled=true` 控制，僅提供明確診斷用 `--skip-shadow-strength-report` 逃生開關。專案 `AGENTS.md` 與可恢復使用者偏好已同步；原 rise score 保留為背景欄位，四項分數仍為 `watch_only`，不能覆蓋市場風險 gate 或正式策略。
- 新增每日四項影子強度套用器 `scripts/score_zhu_walkline_daily_shadow_strength.py`：讀取指定日期 scanner feature matrix，僅納入 `trade_date == asof_date` 的新鮮 `KD_OVERSOLD_TREND_RECOVERY` 確認事件，使用已凍結 Jan-Mar 門檻與嚴格早於訊號日的主力 proxy、無上影、量比、融資 5 日變化資料，輸出 CSV／JSON／Markdown。2026-07-13 本機價格、籌碼、融資與產業資料均到當日；scanner 為 `MARKET_HIGH_RISK_BREAKDOWN`、正式多方觀察名單 0 筆，但有 5 筆新鮮 KD 確認事件可做 shadow 排名：9934、6944 各 75 分，1434 為 50 分，9921、6517 各 25 分。全部 `watch_only`，不產生正式交易效果。
- 新增四項等權 KD D+5 訊號日前影子強度分數 `src/abc_quant/features/shadow_strength.py`：主力 proxy、無上影供給、前一日 20 日量比、融資餘額 5 日變化各 25 分，合計 0～100；門檻只由 2026-01～03 發現期決定，2026-04～06 固定驗證。明確排除原始融資餘額與外資買賣超原始股數；任何一項缺失即 `INSUFFICIENT_FEATURES`，不補零、不排名。排名母體涵蓋 2026H1 全部 909 筆訊號（827 筆完整），包含 0～<10% 中性結果，避免用未來 D+5 分組選樣；主要驗證 scope 完整覆蓋 430/475，holdout 完整覆蓋 230/247。累積門檻 `>=10%` 命中率由全完整樣本 30.87% 單調升至 100 分 47.62%，`>=20%` 由 13.91% 升至 28.57%，loss 由 69.13% 降至 52.38%。精確分箱不完全單調且 75/100 分報酬中位數仍為負，故只保留 shadow 排序，不是校準機率或交易 gate。輸出規則、holdout validation、全期間排名及最新訊號日排名 CSV；`formal_champion_changed=False`、`formal_trade_effect=False`、`blocked_before_promotion_review`。
- 新增 KD D+5 訊號日前籌碼／量價特徵 sidecar `scripts/analyze_zhu_walkline_kd_d5_pre_signal_features.py` 與可重用模組 `src/abc_quant/features/pre_signal_features.py`：對 641 筆指定三組事件加入嚴格 `feature_date < signal_date` 的前期量比、窄幅 K 棒、外資／投信／自營商買賣超占成交量、主力與買賣家數差 proxy、TDCC 千張大戶、融資變化等 38 個數值欄位；缺失不補零，並輸出 all-events、排除公司行動、同股 5 日 cooldown、兩者合併四個 scope。主要 scope 475 rows；法人 100%、主力 proxy 99.38%、TDCC 50.55%、融資 91.42% 覆蓋。Jan-Mar 固定門檻套 Apr-Jun 僅作特徵優先順序：訊號前一日主力 proxy 對 D+5 >=10% 的 precision lift 1.270，訊號前一日量比對 >=20% lift 1.148；外資原始股數與成交量正規化方向不一致、TDCC 覆蓋不足，均不得直接成為硬門檻。修正 FinLab items config 的重複 `history/history` 路徑。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`、`blocked_before_promotion_review`。
- 新增 Zhu walkline KD D+5 三組特徵分析 sidecar `scripts/analyze_zhu_walkline_kd_d5_groups.py`：以訊號日後第 5 個個股交易日的還原收盤計算報酬，互斥分成 `D5_LOSS`、`D5_GAIN_10_20`、`D5_GAIN_GE_20`，另將 0% 至未滿 10% 保留為 ungrouped，避免扭曲使用者指定三組。`2026-01-01`~`2026-06-30` 共 909 筆 fresh KD confirmation，907 筆成熟 D+5 標籤；三組為 466／88／87 筆，另 266 筆落在 0%~10%、2 筆缺 D+5。輸出完整逐筆清單、每組股票 CSV、同股 5 日 cooldown、排除公司行動敏感度、數值差異、類別 lift、pairwise contrasts、unique-stock summary、JSON 與 Markdown。20% 以上組相較虧損組最明顯為較高的股價相對月線乖離、20 日動能、月線斜率、K 值與量能擴張；這也是較高追價與可成交性風險的描述，不構成進場規則。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline KD(9,3,3) 超賣後轉強 shadow 規則：`K < 20` 僅標記 `OVERSOLD_ONLY`，不視為止跌或轉強；後續必須同時具備近 5 日曾超賣、K 向上、近 5 日曾實際向上突破 D 且目前 K 位於 D 上方、價格站回前高／20 日線／壓力區、多頭趨勢閘門（20/60 日線斜率向上且股價在 60 日線上）、強勢股閘門（股價在 20/120 日線上且 20 日報酬為正），並排除放量上影供給，才標記 `KD_OVERSOLD_TREND_RECOVERY` / `CONFIRMED`。CSV、summary JSON、shadow log 與個股 Markdown 均輸出 KD 數值、階段與各 gate。以本機 2464 驗證：2026-06-11、06-12 僅為超賣標記；2026-06-15 才完成 K 上彎、K>D、價格收復壓力與多頭強勢 gate。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Yahoo 台股概念股重要快照與 Zhu 分層 shadow 閘門：`scripts/sync_yahoo_concept_membership.py` 由 Yahoo `class#CONCEPT_STOCK` 及 `StockServices.getClassQuotes` 分頁 API 下載 101 個概念、2,724 筆概念－個股關係、1,063 檔唯一股票，保存為 append-only SQLite snapshot `yahoo_concept_2026-07-09_b17edf05cd17`（SHA-256 `b17edf05...aad63`、`IMPORTANT_BASELINE`）。新增 as-of 概念廣度：站上20日線比率、20日線正斜率比率、5日正報酬比率與概念強度，並將 driver screen 固定為 `大盤 -> 類股 -> 概念股 -> 個股 driver_score`；歷史倒灌明確標記 `static_current_backfill_user_authorized`。canonical `2026-06-01`~`2026-07-09` 原 39 rows 經分層後為 15 observation rows；`5488 松普` 雖 score 12，但 7/2「華為」概念為 `CONCEPT_WEAK`，不再通過。延伸 `2026-01-01`~`2026-07-09` canonical 2,875 source rows，driver-only 286 observations、hierarchy 154 observations；成熟 D+20 140 rows 相較同日期同數量 driver-score control，平均 +0.359 pct、20% hit +0.714 pct、tail loss -3.572 pct，僅保留 shadow 風險過濾，未通過 promotion。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 依 canonical 策略契約重跑 `2026-06-01`~`2026-07-09` Zhu driver screen：上游 `fast_precomputed_daily_ohlcv` 每日最多 30 檔（`max_per_day=30`），再套 `driver_score >= 11`，不加 MA5 cap、cooldown 或 forward-return 條件。28 個交易日共 778 source rows，最終 39 observation rows、34 unique stocks、9 signal dates；7 月僅 `2026-07-02 / 5488 松普 / score 12` 一筆。輸出 canonical candidates、scored universe、daily counts、date-stock codes 與 summary JSON/MD 至 `reports/zhu_walkline_driver_screen_candidates_2026_06_01_07_09/`。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 依使用者指定口徑將 `2026-07-01`~`2026-07-09` Zhu driver screen canonical 顯示退回原策略母體：上游使用預設 `fast_precomputed_daily_ohlcv`、`max_per_day=30`，先保留每日前 30 個 observation candidates，再套 `driver_score >= 11`。重跑 7 個交易日共 180 source rows，最終只有 `2026-07-02 / 5488 松普 / close 14.05 / driver_score 12` 一筆；失效觀察價 13.05、下一確認壓力 14.3。前一輪 `max_per_day=0` 的 2,031-row 寬母體與 76-row MA5-cap 結果保留為非 canonical 研究存檔，不再用於本策略日期清單。程式預設本來就是 30，因此不需回退安全的 empty-rolling schema 修正。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 執行 Zhu walkline `2026-06-01`~`2026-07-09` shadow window 並修正短區間空 rolling 報告：`compute_rolling_window_metrics(...)` 在成熟交易日少於 rolling window 時改回傳固定空 schema，避免 Markdown 產生 `KeyError: window_end_date`，新增 regression test。區間本機 `daily_ohlcv_features` / `tw_adjusted_ohlcv_daily` 均新鮮至 `2026-07-09`；28 個交易日輸出 2,031 source candidates，`driver_score >= 11` 為 117 rows，套用已 backward-OOS 複驗的 `close_to_sma5_pct <= 12` 後為 76 observation rows、60 stocks、9 signal dates，最新訊號日 `2026-07-02`，7/3~7/9 無完整門檻訊號。window-local 20 日 cooldown 為 60 rows，其中僅 23 rows / 2 signal dates 已有 D+20，成熟度不足；即使成熟樣本 net avg 4.231%，median -6.216%，仍標示 `insufficient_mature_horizon`、`watch_only`、`blocked_before_promotion_review`。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 強化 Zhu walkline shadow strategy experiment 的實務可成交性與獨立複驗：新增 evaluator-only `entry_adj_high/low/previous_close`、`entry_return_vs_previous_close_pct`、`entry_locked_limit_up`，並輸出 `cooldown_20d_buyable_entry` / buyable-no-corporate-action robustness scopes；隔日一字漲停只影響 evaluator scope，不參與 signal membership。新增事前指定規則複驗介面與 `BASELINE_EXCLUDE_STRONG_UPTREND` 探索變體，使用未參與上一輪規則發現的 2019~2021 點時候選（56,098 rows、1,736 stocks、731 trading days）做 2019 development / 2020 validation / 2021 locked holdout。`BASELINE_MA5_GAP_CAP_12` 在原 cooldown 與排除一字漲停兩種 scope、2020/2021 共四組皆改善中位數、downside 與 tail risk，平均報酬差容許在 -0.25 pct 內，判為 `shadow_replication_supported`；但仍不得 promotion。`BASELINE_EXCLUDE_STRONG_UPTREND` 四組皆失敗，顯示近期效果不可跨 regime 固定化。2022~2026 主實驗重跑後仍由 validation 選 `BASELINE_LIQUIDITY_20M`，三折 walk-forward 仍 0/3，`blocked_before_promotion_review`。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增完整 Zhu walkline shadow strategy experiment sidecar `scripts/experiment_zhu_walkline_strategy.py` 與 10 項 focused tests：合併 `2022-01-03`~`2026-06-10` 共 79,969 筆 as-of 候選，使用 `tw_adjusted_ohlcv_daily` 以訊號次日 adjusted open 進場、D+20 adjusted close 評估，納入雙邊各 0.1425% 手續費、賣出 0.3% 證交稅、雙邊各 0.1% 滑價、20 交易日同股 cooldown、公司行動 robustness、同日同檔數 top-rise/random baselines、有限變體比較、季度失敗歸因與三折年度 walk-forward。基準在 2022~2024 development 的 net avg/median/tail<=-10% 為 -1.390%/-2.945%/25.064%，2025 validation 為 4.955%/0.868%/20.595%，2026H1 holdout 為 10.086%/3.280%/20.359%；2025 選出的 `BASELINE_LIQUIDITY_20M` 在 2026H1 雖提高平均與中位數，tail loss 卻升至 23.472%，三折 holdout 最終 0/3 通過，`blocked_before_promotion_review`。`BASELINE_MA5_GAP_CAP_12` 只列 post-holdout risk-control research clue，需新資料複驗；訊號日 late-chase/upper-tail/volume-exhaustion 三旗標對基準為 no-op，應留在日後高點/持有生命週期監測，不回灌初始篩選。另將 forward-return bucket 的 Spearman 改為 rank 後 Pearson 等價計算，移除未宣告 SciPy 依賴。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline driver screen overlay 與 rolling backtest sidecar `scripts/backtest_zhu_walkline_driver_screen.py`：依 forward-return bucket 研究結論建立 as-of-only 分數篩選規則，預設 `driver_score >= 11`，規則包含 `電子零組件` 類股集中、嚴格區間突破、攻擊量、紅K收高、短均乖離、類股領先、低風險確認與 clean review；再以 20 交易日後 forward return 做 evaluator-only rolling backtest。`2026-01-01`~`2026-06-30` 全候選 12,060 valid evaluator rows 中，driver screen 保留 1,084 rows、256 unique stocks、62 dates，平均 20日後報酬 14.518%、中位數 6.460%、`>=20%` hit rate 30.258%、`>=50%` hit rate 13.007%；同日同檔數 top-rise baseline 平均 8.944%、`>=20%` hit rate 20.019%、`>=50%` hit rate 6.827%。本 overlay 改善平均報酬與大漲命中率，但 `<=-10%` tail-loss rate 17.343% 高於 top-rise baseline 13.469%，因此仍是 shadow research，blocked before promotion review。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline forward-return bucket research sidecar `scripts/analyze_zhu_walkline_forward_return_buckets.py`：將已通過 20 交易日後漲幅 `>=20%` 的 2,632 rows 分成 `21%-30%`、`31%-40%`、`41%-50%`、`>50%` 四桶，輸出 bucket rows、bucket summary、numeric feature stats、category lift、reason drivers、summary JSON/MD 至 `reports/zhu_walkline_forward_return_bucket_research_2026_01_06/`。本輪量化結果：`21%-30%` 988 rows、`31%-40%` 560 rows、`41%-50%` 366 rows、`>50%` 718 rows；`>50%` 桶最明顯的類別驅動是 `sector=電子零組件`，bucket share 40.11% vs all share 29.41%，lift 1.364，數值面則偏向較高短均乖離、較強當日紅K/收盤位置與較高量比。此為 evaluator-only 事後特徵歸因，不是 live selection feature。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 強化 Zhu walkline early observation 人工標註 sidecar：新增 evaluator-only forward close label 欄位 `forward_close_date`、`forward_close`、`forward_return_pct`，並提供 `--min-forward-return-pct` / `--forward-return-trading-days` 只用於回測標註篩選；本輪依使用者要求以 20 個交易日後收盤漲幅 `>=20%` 過濾 `2026-01-01`~`2026-06-30` 候選，輸出至 `reports/zhu_walkline_early_observation_labels_2026_01_06_fwd20p/`。過濾前 13,116 rows，缺 20 日後收盤資料 1,056 rows，刪除 10,484 rows，保留 2,632 rows、523 unique stocks、76 個非空候選日；稽核確認 `forward_return_pct < 20` 與缺值列皆為 0，且未輸出 `NaN`/`nan`/`None`/`<NA>` 字串。forward return 是 hindsight evaluator 欄位，不是 live selection feature。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline early observation 人工標註 sidecar `scripts/export_zhu_walkline_early_observation_candidates.py`：支援 `--engine exact` 使用完整 scanner，並新增 `--engine fast` 使用本地 `daily_ohlcv_features` 預先計算日線欄位快速產生人工標註候選；輸出 full candidates、label todo、date/stock code 簡表、daily counts、summary JSON/MD。候選已加入硬門檻 `close > ma20`、`ma20_slope > 0`、`close > ma120`，股價未站上月線/半年線或月線斜率未翻正即刪除。fast engine 預設 lookback 改為 260 calendar days 以計算 120 日均線。已用 fast engine 重跑 `2026-01-01`~`2026-06-30`，解析交易日 `2026-01-02`~`2026-06-30` 共 116 日、13,116 candidate rows、1,433 unique stocks、90 個非空候選日；使用 `include_etf_like=false` 排除 `00xx` ETF-like 代碼，稽核確認 `close<=ma20`、`ma20_slope<=0`、`close<=ma120` 與缺值列皆為 0。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline report tone spec `CODEx_WALKLINE_REPORT_TONE_SPEC.md`，並將個股 Markdown 報告改為固定「走圖教學式」十三章順序：先講結論、趨勢、均線、K棒、量能、法人籌碼、大戶/主力 proxy、融資券、支撐壓力、明日劇本、未持有者、已持有者、一句話；報告固定標示技術分析教育與 shadow observation，使用觀察/防守/訊號失效語言，不冒充本人、不輸出絕對買賣建議。`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 修正 Zhu walkline shadow range backtest sidecar 摘要口徑：forward evaluator 的不足 horizon 改列 missing，不再算成 miss；summary 同時輸出 daily equal-weighted metrics、row-weighted metrics、valid/missing row counts、baseline metrics、excess vs baseline 與 monthly metrics；新增 all-market、fixed-seed random same-count、score-decile baselines，並釐清 fall downside/adverse rally 語意。已重跑 2026-01-01~2026-05-31，本地實際交易日 `2026-01-02`~`2026-05-29` 共 95 日、5,368 evaluator rows、2,280 baseline rows、10 monthly rows，`max_future_date_used=2026-06-23`；`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 修正 Zhu walkline observation points 的 shadow-only 邊界與輸出語意：`ZhuWalklineResult.mode` 硬鎖 `shadow_observation_only`，非 shadow config 只寫入 run_notes warning；新增 `buy_trigger_price_role`、`buy_observation_detail_types`、`sell_warning_detail_types`，主型態欄位收斂為單一優先型態；嚴格化 `RESISTANCE_TURN_SUPPORT`、避免價跌量增單獨誤標 `SUPPORT_BREAKDOWN`、過濾低於現價的 target resistance，並補齊 `stop_reference` 至 bullish/fall/shadow log/summary JSON。Markdown 改成觀察語言並明示「不是買進名單，不是賣出指令，僅為支撐壓力觀察價與訊號失效價。」`mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`；no formal strategy modified、no formal champion modified、no formal trade effect。
- 新增 Zhu walkline 支撐壓力買賣觀察點欄位：`buy_observation_type`、`buy_trigger_price`、`target_resistance_1/2`、`sell_warning_type`、`invalidation_price`，支援 `SUPPORT_REBOUND`、`RESISTANCE_BREAKOUT`、`RESISTANCE_TURN_SUPPORT`、`FAILED_BREAKDOWN_RECLAIM` 與 `RESISTANCE_REJECTION`、`SUPPORT_BREAKDOWN`、`ATTACK_K_FAILURE`、`FALSE_BREAKOUT`、`MA_SUPPORT_FAILURE`；輸出至 CSV、summary JSON、shadow log 與 Markdown 報告，仍為 shadow observation / 教學觀察欄位，不產生交易指令。
- 強化 Zhu walkline 支撐/壓力計算：將單點支撐壓力升級為 1.5% 內合併的 `support_zone_*` / `resistance_zone_*` 區間，來源納入前高低、3/5/20/60日高低、5/10/20/60日均線、大量紅黑K、長上下影線、跳空缺口與整數關卡；新增支撐有效/失敗、壓力有效突破/假突破欄位，報告與 summary JSON 同步輸出 zone records，正式策略與交易效果仍不變。
- 強化 Zhu walkline shadow scanner 訊號紀律：將主要輸出改為 `top_bullish_watchlist`/「多方轉強觀察股」並保留 legacy `top_rise_candidates`，新增 `signal_stage`、`trigger_type`、`invalid_price`、`confirm_price`、`failure_type`、`reversal_state` 與 shadow log；加入大盤弱勢分級上限、高檔爆量長上影供給壓力、法人買超價弱背離、跌破20日線/支撐且融資增加風險，並固定 `mode=shadow_observation_only`、`formal_champion_changed=False`、`formal_trade_effect=False`。
- 新增 direct task Zhu walkline shadow scanner：保存原始任務書 `CODEx_ZHU_WALKLINE_SHADOW_TASK.md`，新增 `config/zhu_walkline_shadow.yaml`、概念股 mapping、local SQLite loader、走圖技術特徵、法人/大戶proxy/融資券/市場與概念輪動、官方重大訊息 web cache 補充、shadow scoring、Markdown/CSV/JSON/parquet 報告與 `scripts/run_zhu_walkline_shadow.py`；預設 `--no-web`，`--use-web` 僅補充官方 mirror 事件，不修改 formal champion / formal weights / formal trades；新增 no-lookahead、feature、web cache focused tests，並加入 `pyarrow` 依賴以輸出 parquet。
- 新增 Task 055 walk-forward constant-baseline evaluation smoke diagnostics：`run_walk_forward_baseline_smoke(...)` 在 deterministic walk-forward supervised windows 內串接 train-only scaler、supervised dataset、constant baseline、prediction bundle 與 evaluation helpers，回傳 per-window baseline value / label-drop counts / train-validation-test metrics；新增 summary validator/constants，不調用 LightGBM、不做 model selection/strategy/backtest。
- 新增 Task 053 walk-forward supervised dataset smoke diagnostics：`run_walk_forward_supervised_smoke(...)` 串接 deterministic smoke frame、FeatureMatrix、WalkForwardSplitPlan、train-only scaler 與 supervised dataset builder，回傳 per-window split counts / label-drop counts / scaler feature count metadata；新增 summary validator/constants，不 fit model、不做 walk-forward model evaluation、不新增策略或回測行為。
- 新增 Task 052 deterministic walk-forward split contract：`WalkForwardWindow` / `WalkForwardSplitPlan` 與 `build_walk_forward_split_plan(...)` 定義 expanding-train、rolling validation/test 的 deterministic integer-position windows，`validate_walk_forward_split_plan(...)` 驗證 contiguous/no-overlap/bounds/JSON-friendly contract；不讀資料、不 fit model、不改 temporal split、不新增策略或回測行為。
- 新增 Task 051 optional LightGBM evaluation smoke diagnostics：`run_lightgbm_evaluation_smoke(...)` 預設只回 dependency/default-parameter diagnostics，`fitting_enabled=True` 且 optional package 可用時才透過既有 train-only `fit_lightgbm_regressor(...)` 產生 in-memory train/validation/test evaluation summary；新增 summary validator/constants 與 focused tests，不改 dependency-smoke/CLI outputs、不新增 mandatory dependency。
- 新增 Task 050 LightGBM dependency smoke summary contract package exports：`abc_quant.pipeline` 直接匯出 `LIGHTGBM_DEPENDENCY_SMOKE_*` constants 與 `validate_lightgbm_dependency_smoke_summary(...)`，讓 callers 可用 public pipeline import 驗證 diagnostics summary，不改輸出 schema、不改 validator semantics。
- 新增 Task 049 LightGBM dependency smoke summary contract validator：集中定義 diagnostics summary/default params key constants 與 `validate_lightgbm_dependency_smoke_summary(...)`，`run_lightgbm_dependency_smoke(...)` 回傳前會驗證 shape、JSON-friendly values 與 forbidden keys，不改輸出 schema、不 require/fitting/search/select/strategy/backtest。
- 新增 Task 048 packaged LightGBM dependency smoke command alias：`pyproject.toml` 宣告 `abc-quant-lightgbm-dependency-smoke = abc_quant.cli.lightgbm_dependency_smoke:main`，並以 `tomllib` 測試 entry point target 可解析、可呼叫與支援 `--indent 2`，不改 diagnostics behavior、不新增 mandatory LightGBM dependency。
- 新增 Task 047 deterministic LightGBM dependency smoke module CLI：`python -m abc_quant.cli.lightgbm_dependency_smoke` 以 sorted JSON 輸出既有 `run_lightgbm_dependency_smoke(...)` summary，支援 `--indent`，不新增 packaged console-script alias、不 require 真套件、不 call `require_lightgbm()` default path、不 fit/search/select/strategy/backtest。
- 新增 Task 046 deterministic LightGBM dependency smoke diagnostics：`run_lightgbm_dependency_smoke(...)` 只使用 `check_lightgbm_dependency()` 回報 optional package status 與 default params metadata，預設 `fitting_enabled=False`，不 require 真套件、不 fit model、不做 parameter search/model selection/strategy/backtest。
- 新增 Task 045 train-only LightGBM regressor fitting contract：`fit_lightgbm_regressor(...)` 透過 optional `require_lightgbm()` 匯入 `lightgbm`，只用 `SupervisedSplitDataset.train_X/train_y` fit，並將 train/validation/test features 轉成 `SplitPredictionBundle`；測試以 fake LightGBM monkeypatch 覆蓋，不新增 mandatory dependency、不做 parameter search/model selection/strategy/backtest。
- 新增 Task 044 optional LightGBM dependency guard and parameter contract：`models/lightgbm.py` 可在未安裝 `lightgbm` 時 import，並以標準函式庫 `importlib` 提供 dependency status、required import helper 與 frozen deterministic regressor parameter validation；不新增 mandatory dependency、不 fit model、不改 pipeline/CLI/smoke outputs。
- 新增 Task 043 packaged model comparison smoke console-script alias：`pyproject.toml` 宣告 `abc-quant-model-comparison-smoke = abc_quant.cli.model_comparison_smoke:main`，並以 `tomllib` 測試 entry point target 可解析、可呼叫與支援 `--baseline-method median`。
- 新增 Task 042 deterministic model comparison smoke diagnostics CLI：`python -m abc_quant.cli.model_comparison_smoke` 以 sorted JSON 輸出既有 `run_model_comparison_smoke(...)` summary，支援 `--train-end`、`--validation-end`、`--baseline-method mean|median` 與 `--indent`，不改計算、summary keys、model selection/ranking/strategy/simulation 行為。
- 新增 Task 041 model comparison smoke summary contract validator：集中定義 `MODEL_COMPARISON_SMOKE_*` summary/model/split/comparison keys 與 `validate_model_comparison_smoke_summary(...)`，`run_model_comparison_smoke(...)` 回傳前會驗證 summary shape，不改預設輸出值、不加入 model selection/ranking/strategy/simulation。
- 新增 Task 040 deterministic baseline versus OLS evaluation comparison smoke diagnostics：`run_model_comparison_smoke(...)` 在相同 supervised prediction rows 上評估 constant-baseline reference 與 OLS candidate，並用 `compare_prediction_evaluations(...)` 回傳 raw metric deltas，不做 winner、ranking、decision 或 model selection。
- 新增 Task 039 prediction evaluation comparison contract：`compare_prediction_evaluations(...)` 比較兩個既有 `SplitPredictionBundleEvaluationResult`，驗證 split row/missing counts 一致後回傳 candidate-minus-reference 的 MAE、RMSE、mean error 與 prediction mean deltas，不做 model selection、ranking、strategy 或 simulation。
- 新增 Task 038 packaged OLS smoke console-script alias：`pyproject.toml` 宣告 `abc-quant-linear-regression-smoke = abc_quant.cli.linear_regression_smoke:main`，並以 `tomllib` 測試 entry point target 可解析與呼叫。
- 新增 Task 037 deterministic OLS smoke diagnostics CLI：`python -m abc_quant.cli.linear_regression_smoke` 以 sorted JSON 輸出既有 `run_linear_regression_smoke(...)` summary，支援 `--train-end`、`--validation-end` 與 `--indent`，錯誤邊界會以 non-zero exit code 與簡短 stderr 回報。
- 新增 Task 036 linear regression smoke summary contract validator：集中定義 `LINEAR_REGRESSION_SMOKE_SUMMARY_KEYS`、`LINEAR_REGRESSION_SMOKE_SPLITS`、`LINEAR_REGRESSION_SMOKE_EVALUATION_KEYS` 與 `validate_linear_regression_smoke_summary(...)`，`run_linear_regression_smoke(...)` 回傳前會驗證 OLS diagnostics summary shape，不改預設輸出值。
- 新增 Task 035 deterministic train-only OLS smoke diagnostics：`run_linear_regression_smoke(...)` 串接 deterministic smoke frame、FeatureMatrix、TemporalSplit、train-only scaler、SupervisedSplitDataset、`fit_linear_regression(...)` 與 `evaluate_prediction_bundle(...)`，回傳 JSON-friendly OLS diagnostics，不產生 strategy/allocation/performance/simulation outputs。
- 新增 Task 034 train-only ordinary least-squares regression model contract：`fit_linear_regression(...)` 只使用 `SupervisedSplitDataset.train_X/train_y` 以 `numpy.linalg.lstsq` 擬合，並回傳 `LinearRegressionResult` 與 `SplitPredictionBundle`，不讀 validation/test labels、不新增 sklearn。
- 新增 Task 033 packaged supervised dataset smoke console-script alias：`pyproject.toml` 宣告 `abc-quant-supervised-smoke = abc_quant.cli.supervised_smoke:main`，並以 `tomllib` 測試 entry point target 可解析與呼叫。
- 新增 Task 032 supervised dataset smoke diagnostics CLI：`python -m abc_quant.cli.supervised_smoke` 以 sorted JSON 輸出既有 supervised dataset smoke summary，支援 `--train-end`、`--validation-end` 與 `--indent`，錯誤邊界會以 non-zero exit code 與簡短 stderr 回報。
- 新增 Task 031 supervised dataset smoke summary contract validator：集中定義 `SUPERVISED_DATASET_SMOKE_SUMMARY_KEYS`、`SUPERVISED_DATASET_SMOKE_SPLITS` 與 `validate_supervised_dataset_smoke_summary(...)`，`run_supervised_dataset_smoke(...)` 回傳前會驗證 summary shape，不改預設輸出值。
- 新增 Task 030 deterministic supervised dataset smoke diagnostics：`run_supervised_dataset_smoke(...)` 串接 deterministic smoke frame、feature matrix、temporal split、train-only scaler 與 supervised split dataset，回傳 JSON-friendly label-drop diagnostics，不訓練 estimator、不改既有 smoke CLI 或 summary。
- 新增 Task 029 supervised split dataset contract：`SupervisedSplitDataset` 與 `build_supervised_split_dataset(...)` 將 standardized split features 與 `FeatureMatrix` labels 對齊，支援 split-level missing-label drop counts，並拒絕 missing-label no-drop 與 empty train data，不訓練 estimator。
- 新增 Task 028 packaged preprocessing smoke console-script alias：`pyproject.toml` 宣告 `abc-quant-preprocessing-smoke = abc_quant.cli.preprocessing_smoke:main`，並以 `tomllib` 測試 entry point target 可解析與呼叫。
- 新增 Task 027 preprocessing smoke diagnostics CLI：`python -m abc_quant.cli.preprocessing_smoke` 以 sorted JSON 輸出既有 preprocessing smoke summary，支援 `--train-end`、`--validation-end` 與 `--indent`，錯誤邊界會以 non-zero exit code 與簡短 stderr 回報。
- 新增 Task 026 preprocessing smoke summary contract validator：集中定義 `PREPROCESSING_SMOKE_SUMMARY_KEYS`、`PREPROCESSING_SMOKE_SPLITS` 與 `validate_preprocessing_smoke_summary(...)`，`run_preprocessing_smoke(...)` 回傳前會驗證 top-level keys、split counts 與 split shape，不改預設輸出值。
- 新增 Task 025 deterministic preprocessing smoke diagnostics：`run_preprocessing_smoke(...)` 串接 deterministic smoke frame、feature matrix、temporal split、train-only scaler fit 與 transform，回傳 JSON-friendly scaling diagnostics，不改 modeling smoke CLI 或 summary contract。
- 新增 Task 024 train-only numeric feature standardization contract：`fit_standard_scaler(...)` 只用 train split 擬合 means/stds，再由 `transform_with_standard_scaler(...)` 套用到 train/validation/test，拒絕 nonnumeric、missing train values 與 zero-variance training features，不新增 sklearn 或 estimator。
- 新增 Task 023 modeling smoke pipeline bundle-evaluation wiring：`run_baseline_modeling_smoke(...)` 內部改用 `build_constant_baseline_prediction_bundle(...)` 與 `evaluate_prediction_bundle(...)`，保持 summary contract、CLI arguments、fitted values、split counts 與 metric formulas 不變。
- 新增 Task 022 split prediction bundle evaluator：`SplitPredictionBundleEvaluationResult` 與 `evaluate_prediction_bundle(...)` 使用既有 `evaluate_predictions(...)` 評估 train/validation/test bundle outputs，保留 `model_name`/`method`，不改 pipeline、CLI 或 summary keys。
- 新增 Task 021 constant-baseline prediction bundle adapter：`build_constant_baseline_prediction_bundle(...)` 將既有 `ConstantBaselineResult` 轉成 `SplitPredictionBundle`，沿用 generic bundle validation 與 copy isolation，不改 baseline、pipeline、CLI 或 summary keys。
- 新增 Task 020 split prediction bundle diagnostics contract：`SplitPredictionBundle` 與 `build_split_prediction_bundle(...)` 固定 train/validation/test prediction Series 形狀，拒絕 duplicate/missing/overlap inputs，並回傳 copied Series，不改 baseline、CLI 或 summary keys。
- 新增 Task 019 packaged console-script alias：`pyproject.toml` 宣告 `abc-quant-modeling-smoke = abc_quant.cli.modeling_smoke:main`，並以 `tomllib` 測試 entry point target 可解析與呼叫。
- 新增 Task 018 modeling smoke baseline method selector：`run_baseline_modeling_smoke(...)` 與 CLI 支援既有 constant-baseline `mean`/`median` method，summary 新增 `baseline_method` 並同步更新 shared contract，不改 split 或 metric 公式。
- 新增 Task 017 modeling diagnostics summary contract validator：集中定義 `MODELING_SMOKE_SUMMARY_KEYS`、`EVALUATION_METRIC_KEYS` 與 `validate_modeling_smoke_summary(...)`，pipeline 回傳前會驗證 summary shape，CLI/pipeline tests 改用共用 constants。
- 新增 Task 016 deterministic modeling smoke diagnostics CLI：`python -m abc_quant.cli.modeling_smoke` 以 sorted JSON 輸出既有 smoke summary，支援 `--train-end`、`--validation-end` 與 `--indent`，錯誤邊界會以 non-zero exit code 與簡短 stderr 回報。
- 新增 Task 015 deterministic baseline modeling smoke pipeline：`run_baseline_modeling_smoke(...)` 串接 feature matrix、temporal split、constant baseline 與 prediction evaluation，回傳 plain diagnostic summary，不建立 market-action outputs、allocation logic、performance curves 或 simulation engines。
- 新增 Task 014 prediction evaluation metrics：`evaluate_predictions(...)` 與 `evaluate_constant_baseline(...)` 對 split-aligned predictions 計算 row/missing counts、MAE、RMSE、mean error 與 prediction mean，且不產生交易訊號、部位、equity curve 或 backtest。
- 新增 Task 013 constant-baseline model contract：`fit_constant_baseline(...)` 支援 mean/median 常數 baseline，只使用 train split 的非缺失 label fit，並回傳 train/validation/test positional predictions，避免 validation/test label 洩漏。
- 新增 Task 012 temporal split contract：`build_temporal_split(...)` 支援 train/test 與 train/validation/test date-based split，防止 training dates 洩漏到 validation/test，並覆蓋 shuffled metadata invariance、non-increasing boundary、empty split 與 missing/unsortable date tests。
- 新增 Task 011 feature-matrix assembly contract：`build_feature_matrix(...)` 將 `X`、明確 `y`、`date`/`ticker` metadata 分離，排除 raw OHLCV 與 `label_` 欄位，並覆蓋 shuffled-input invariance 與 missing-label preservation。
- 新增 Task 010 pure-pandas technical indicators：`add_technical_indicators(...)` 支援 SMA、EMA、RSI，並以 no-lookahead、ticker isolation、shuffled-input invariance 與 future-row mutation tests 覆蓋。
- 新增 Task 009 feature/label leakage regression tests，覆蓋 shuffled input invariance、multi-ticker isolation、rolling volatility 數值、forward-return label tail missing 與 label 不混入 feature columns。
- 強化 market data validation，改用 schema constants 並拒絕 non-numeric/missing OHLCV、negative volume、high-low 反轉，以及 open/close 超出 high-low range。
- 新增 deterministic market data contract、合成 sample fixture 與端到端 smoke pipeline，串接資料驗證、價量特徵、forward-return label 與 basic metrics summary。
- 新增最小 GitHub Actions CI workflow，於 pull request 與 push to main 執行 ruff、pytest、compileall，並使用 Python 3.11/3.12 matrix。
- 清空 Task 005 後的 `INBOX.md` active task，讓 closed-loop guard 回到 `status=no_task`，避免 PR #3 merge 後重複執行同一任務。
- 準備未來 CI workflow governance target：closed-loop guard 設定允許 `.github/`，測試確認 `.github/workflows/ci.yml` 可作為 target 且 `.git/config` 仍被封鎖。
- 修正 review package final audit，將 output file 從 repo diff-check 中排除，並單獨驗證輸出檔沒有 trailing whitespace。
- 調整 review package SHA metadata，改用 `source_head_sha_at_generation` 與 `review_package_commit_sha: unavailable_self_reference`，避免把 package 產生前 SHA 誤標為最終 head。
- 保留 closed-loop guard 內建 blocked content/path patterns；config 現在只能增加 blockers，不能移除 `token`、`.git`、`data/raw` 等預設防線。
- 強化 Codex closed-loop guard：即使 `risk_level: normal`，仍會封鎖破壞性、憑證、外部網路、絕對路徑、repo 外路徑與資料原始區目標。
- 擴充 `scripts/build_review_package.py`，支援完整 diff、完整檔案內容、驗證結果、完整 HEAD SHA 與排除輸出檔的 `--assert-clean`。
- 新增 tracked review package 生成腳本與本輪 `reviews/review_package_002.md`，取代根目錄一次性 review 輸出。
- 建立 Codex/ChatGPT Pro 檔案式閉環守門器、閉環文件、automation prompt 與測試。
- 完成 ChatGPT Review 001，確認第一輪 scaffold 可接受並記錄下一輪修正 prompt。
- 固定 pytest 本地暫存目錄並停用 cache provider，避免 Windows 受限暫存/cache 目錄造成驗收失敗或警告。
- 新增 `.gitignore`，排除 venv、cache、egg-info、打包 zip、解壓副本與本機 Codex context capsule。

## 0.0.1 - Project bootstrap

- 建立 ABC Quant AI Research Platform 專案骨架。
- 建立 `PROJECT_RULES.md` 作為 Codex 與 ChatGPT 協作規範。
- 建立初始文件、Prompt、程式碼目錄與測試目錄。

## 0.0.2 - First executable research scaffold

- 建立 YAML 設定載入、OHLCV 資料驗證、價量 rolling 特徵、forward return label 與基礎績效指標。
- 新增資料驗證、防未來資料洩漏、label shift 與績效指標測試。
- 2026-07-13: Added `kd_prior_5d_tight_low_volume_gate_v1` to the Zhu walkline shadow KD recovery rule. Confirmation now requires at least one prior-five-session row with open/close body at most 1.2% and existing 20-day volume ratio at most 0.75; the signal row is excluded with `shift(1)`. Formal strategy and trade effects remain unchanged.
- 2026-07-13: Added a one-pass, point-in-time KD observation range sidecar. It exports scanner-compatible confirmation states separately from fresh-price confirmation events so suspended or non-trading stock rows are not misreported as new signal dates.
